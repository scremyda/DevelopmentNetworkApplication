<!DOCTYPE html>

<html>
    <head>
        <link rel="stylesheet" type="text/css" href="static/css/card.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
        <meta charset="UTF-8">
    </head>
 
    <body>
        <div class="search">
            <form class="search-form" action="/companys" method="GET">
            <input type="text" name="company_name" placeholder="Искать здесь..." value="{{.query}}">
            <button type="submit"></button>
            </form>
        </div>
        <div class="container">
            {{if len .card}}
            {{range .card}}
                {{template "product" .}}
            {{end}}
            {{else}}
                <p>По данному запросу ничего не найдено. :(</p>
            {{end}} 
        </div>
    </body>
</html>